apiVersion: v1
kind: Service
metadata:
  name: vision-server
spec:
  type: ClusterIP
  selector:
    app: vision-server
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
