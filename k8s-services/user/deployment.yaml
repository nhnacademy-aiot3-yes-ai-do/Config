apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-server
  template:
    metadata:
      labels:
        app: user-server
    spec:
      serviceAccountName: backend-sa
      containers:
        - name: user-server
          image: ghcr.io/nhnacademy-aiot3-yes-ai-do/user:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: user-config
            - secretRef:
                name: backend-domain-secret
            - secretRef:
                name: auth-secret
            - secretRef:
                name: log-secret
            - secretRef:
                name: postgresdb-secret
            - secretRef:
                name: redis-secret
            - secretRef:
                name: rabbitmq-secret
